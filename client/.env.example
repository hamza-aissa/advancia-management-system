# Vite replaces this value at build time. Use /api behind the demo Nginx proxy.
# For direct `npm run dev`, use http://localhost:5000/api if no dev proxy exists.
VITE_API_URL=http://localhost:5000/api
